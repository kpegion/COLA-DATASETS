description: 'COLA Observational Data Catalog'
sources:

  SH_Monthly:
    args:
      path: "{{CATALOG_DIR}}/sh_monthly.yaml"
    description: 'COLA Sensible Heat Flux Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
    
  LH_Monthly:
    args:
      path: "{{CATALOG_DIR}}/lh_monthly.yaml"
    description: 'COLA Latent Heat Flux (Monthly) Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
    
    
  SH_Daily:
    args:
      path: "{{CATALOG_DIR}}/sh_daily.yaml"
    description: 'COLA Sensible Heat Flux (Daily) Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
    
    
  LH_Daily:
    args:
      path: "{{CATALOG_DIR}}/lh_daily.yaml"
    description: 'COLA Latent Heat Flux (Daily) Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

