description: COLDA SUBX_FORECAST_PSL Data Caalog
sources:
  subx_forecast_psl_daily:
    args:
      path: '{{CATALOG_DIR}}/subx_forecast_psl_daily.yaml'
    description: ''
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
