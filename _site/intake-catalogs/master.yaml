description: 'AOES Master Data Catalog'
sources:

  OBS:
    args:
      path: "{{CATALOG_DIR}}/obs.yaml"
    description: 'AOES Observational Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

  Reanalysis:
    args:
      path: "{{CATALOG_DIR}}/reanalysis.yaml"
    description: 'AOES Reanalysis Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

  Analysis:
    args:
      path: "{{CATALOG_DIR}}/analysis.yaml"
    description: 'AOES Analysis Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

  Model:
    args:
      path: "{{CATALOG_DIR}}/model.yaml"
    description: 'AOES Model Simulations and Experiments Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}