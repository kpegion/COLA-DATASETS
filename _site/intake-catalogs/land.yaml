description: 'AOES Observational Data Catalog'
sources:

  integrated:
    args:
      path: "{{CATALOG_DIR}}/integrated.yaml"
    description: 'AOES  Data Catalog for Datasets Combining Multiple Types of Data'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

    
  topo:
    args:
      path: "{{CATALOG_DIR}}/topo.yaml"
    description: 'AOES Topographic Datasets Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
    
   
  soil_params:
    args:
      path: "{{CATALOG_DIR}}/soil_params.yaml"
    description: 'AOES Soil Parameters Datasets Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

    
  soil_moisture:
    args:
      path: "{{CATALOG_DIR}}/soil_moisture.yaml"
    description: 'AOES Soil Moisture Datasets Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}